spring:
  config:
    import: optional:file:.env[.properties]
  mail:
    host: smtp.gmail.com
    port: 587
    username: bs7819679@gmail.com
    password: ohye walq uxjr ptye
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  application:
    name: melody

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: ${JWT_SECRET}
server:
  port: ${PORT}

algolia:
  app-id: ${ALGOLIA_APP_ID}
  api-key: ${ALGOLIA_API_KEY}
  index-name: ${ALGOLIA_INDEX_NAME}

recombee:
  database-id: ${RECOMBEE_DATBASE_ID}
  secret-token: ${RECOMBEE_DATBASE_SECRET}
  region: ${RECOMBEE_DATBASE_REGION}

audio-processing:
  api:
    key: ${AUDIO_PROCESSING_API_KEY}